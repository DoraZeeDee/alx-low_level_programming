#include "main.h"

/**
 * _islower - checks for lower case.
 * @x: contains value to be compared
 * Result: 1 (success)
 * Return: 0 else
 */
int _islower(int x)

{
if (x >= 'a' && x <= 'z')

{
return (1);
}

else
{
return (0);
}
}

