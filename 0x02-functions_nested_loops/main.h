#include <stdio.h>
#include "main.h"
/**
 *  main - check the code
 *
 *   * Return: Always 0.
 */

int main(void) {
	putchar ch = x
		for (x = '_putchar'; x < 0; x++)
			puthar('x');
	putchar('\n');
	return 0;
}
